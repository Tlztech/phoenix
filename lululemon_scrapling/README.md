# lululemon Scrapling ????

?????????????? Python + Scrapling ?? lululemon ???????????????? Excel?

## ????

1. ??????????? URL
2. ????????????????? URL
3. ????? URL ?? `lululemon_url_YYYYMMDDHHMMSS.xlsx`
4. ????????????????? `output_YYYYMMDDHHMMSS.xlsx`
5. ??????? 3 ?????????? `lululemon_cannotopenurl_YYYYMMDDHHMMSS.xlsx`
6. ????????????? `lululemon_error_YYYYMMDDHHMMSS.txt` ???
7. ?????? checkpoint?????????? `.lululemon_cache/` ???????? URL ??????????

## ????

?? Excel ?????

`title`
`model`
`u_model`
`url`
`brand`
`color`
`size`
`msrp`
`discounted_price`
`stock_status`
`quantity`

???

- `brand` ??? `lululemon`
- `model` ??? `SKU:` ???
- `u_model` ?????????? URL ? `lululemon/` ????????
- `stock_status` ???????????
- `quantity` ??????????`in-stock -> 10`?`low-stock -> 5`?`out-of-stock -> 0`

## ??

```bash
pip install -r lululemon_scrapling/requirements.txt
```

## ??

???????? URL?

```bash
python lululemon_scrapling/main.py
```

???????? 1 ????????? 5 ???????

```bash
python lululemon_scrapling/main.py --seed-limit 1 --detail-limit 5
```

????? URL?

```bash
python lululemon_scrapling/main.py --seed-url "https://www.lululemon.co.jp/ja-jp/c/men" --seed-url "https://www.lululemon.co.jp/ja-jp/c/women/tops"
```

???????

```bash
python lululemon_scrapling/main.py --output-dir ./outputs
```

## ????

?????? `Scrapling` ? `StealthyFetcher`???????????Cloudflare ?????????????????????

?????? `scrapling[fetchers]`??? `StealthyFetcher` ?? Scrapling ? fetcher ?????

## ????

- ?????? HTML ????????? `see-more-button` ?????? URL
- ??????? `digitalData.product.push({...})`?????????????????
- ???????????????? JSON-LD ????

## Quick Start On This Machine

If `python` is not available in `PATH`, use:

```bat
run_scraper.cmd
```

Small end-to-end test:

```bat
run_scraper.cmd --seed-limit 1 --detail-limit 5 --no-resume
```
